import { T } from "../../theme.js";

export const card = { background: T.surface, border: `1px solid ${T.line}`, borderRadius: 14, padding: 14 };

export const primaryBtn = {
  width: "100%", marginTop: 14, background: T.primary, color: "white", border: "none",
  borderRadius: 12, padding: "13px 0", fontFamily: "Inter, sans-serif", fontWeight: 700,
  fontSize: 13.5, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center",
};
